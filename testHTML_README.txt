The corresponding document testHTML.html is a document designed to demonstrate basic HTML/CSS capabilities through the
use of a very basic website. The single page source shows the capabilities of sizing, positioning, color adjustments, image insertions,
and link usage in an HTML site.